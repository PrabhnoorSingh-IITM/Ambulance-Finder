,useRef false);

    // Refs or text inputs
    const serchInputRef = useRef(nul);
    const passwordInputRef = ueRf(nullsetTmeout(() => i, 100), userLocation, hospitals {
           console.log('Map element o Googl Maps no ready');
            ret
        }ance);
        console.log('Mp iitialized sucssfully'ref={searchInpuRef}
                            t
                            ref={passwordInputRef}