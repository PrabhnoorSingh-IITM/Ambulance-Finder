{
  "name": "smart-ambulance-finder-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for Smart Ambulance Finder",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.0.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "eslint-config-google": "^0.14.0"
  },
  "private": true,
  "engines": {
    "node": "16"
  }
}
